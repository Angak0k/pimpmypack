SCHEME=https
HOSTNAME=localhost
DB_HOST=localhost
DB_USER=pmp_user
DB_PASSWORD=pmp1234
DB_NAME=pmp_db
DB_PORT=5432
STAGE=DEV

# JWT Authentication Configuration
API_SECRET=myawsomeapisecret
TOKEN_HOUR_LIFESPAN=1                           # [DEPRECATED] Use ACCESS_TOKEN_MINUTES instead

# Refresh Token Configuration
ACCESS_TOKEN_MINUTES=15                         # Access token lifetime (default: 15 minutes)
REFRESH_TOKEN_DAYS=1                            # Refresh token lifetime (default: 1 day)
REFRESH_TOKEN_REMEMBER_ME_DAYS=30               # Refresh token lifetime with "remember me" (default: 30 days)
REFRESH_TOKEN_CLEANUP_INTERVAL_HOURS=24         # Cleanup interval for expired tokens (default: 24 hours)

# Rate Limiting Configuration
REFRESH_RATE_LIMIT_REQUESTS=10                  # Requests per minute per IP (default: 10)
REFRESH_RATE_LIMIT_WINDOW_MINUTES=1             # Time window in minutes (default: 1)

# Email Configuration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_IDENTITY=PimpMyPack
MAIL_USERNAME=jonh@doe.com
MAIL_PASSWORD=123456
